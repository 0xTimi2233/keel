---
name: implement
description: "基于规范或一组工单实现某项具体工作。"
disable-model-invocation: true
---

实现用户在规范或工单中所描述的工作。

在可能的情况下，在预先约定的接缝处使用 /tdd。

定期运行类型检查、定期运行单个测试文件，并在结束时运行一次完整的测试套件。

完成后，使用 /code-review 审查工作成果。

将你的工作提交 (commit) 到当前分支。
